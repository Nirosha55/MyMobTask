import { createSlice } from "@reduxjs/toolkit";


export const digitalPayment = createSlice({
  name: "DIGITAL_PAYMENT",
  initialState: {},
  reducers: {
    increment: (state, action) => {},
    decrement: (state, action) => {},
  },
});

export const {} = digitalPayment.actions;
export default digitalPayment.reducer;
